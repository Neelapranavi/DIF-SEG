# Local demo data

Generate synthetic image/mask pairs for testing the data pipeline:

```bash
python -m demo.generate_demo_data
```

This data is synthetic and is **not medical data**. It is useful only for checking file loading, preprocessing, training, and application wiring.

Do not use demo results as evidence of medical-model performance.
